import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // 기본 색상
    },
    secondary: {
      main: '#dc004e', // 보조 색상
    },
  },
});

export default theme; 